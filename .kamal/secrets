# Secrets referenced from config/deploy.yml (registry/password, env/secret).
# Everything here is pulled from 1Password at deploy time — this file is safe
# for git precisely because it contains references, never credentials.

KAMAL_REGISTRY_PASSWORD=$(op read op://nplol/registry/password)
HABITAT_SIGNUP_TOKEN=$(op read op://nplol/habitat/signup-token)
